[package]
name = "s3_file_router"
version = "0.1.0"
edition = "2021"

[dependencies]
aws-config = "1.8.14"
aws_lambda_events = { version = "1.0.0", default-features = false, features = ["s3"] }
lambda_runtime = "1.0.1"
tokio = { version = "1", features = ["macros"] }
